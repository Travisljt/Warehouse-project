# Warehouse Management System (WMS)

一个现代化的仓库管理系统，包含仓库控制系统(WCS)功能，支持多仓多货主、物联网设备接入、可视化监控等企业级特性。

## 🏗️ 系统架构

### 技术栈
- **后端**: Spring Boot 3.0.13 + MyBatis-Plus 3.5.9 + Sa-Token
- **前端**: Vue 3 + Vite + Pinia + Vue Router
- **数据库**: PostgreSQL 16
- **缓存**: Redis
- **消息队列**: RabbitMQ
- **网关**: Spring Cloud Gateway
- **前端包管理**: pnpm

### 微服务架构
```
warehouse/
├── backend/                    # 后端微服务
│   ├── common/core/           # 公共模块
│   └── system/                # 业务服务
│       ├── system-auth/       # 认证授权服务
│       └── system-gateway/    # API网关服务
├── frontend/wms-portal/       # 前端应用
├── docs/                      # 文档
└── scripts/                   # 启动脚本
```

## 🚀 快速开始

### 环境要求
- Java 17+
- Node.js 20+
- PostgreSQL 16
- Redis
- RabbitMQ

### 安装依赖
```bash
# 后端依赖 (Maven)
cd backend && mvn clean install

# 前端依赖 (pnpm)
cd frontend/wms-portal && pnpm install
```

### 启动服务

#### 方式1：一键启动所有服务
```bash
./scripts/start-all-services.sh
```

#### 方式2：分别启动
```bash
# 启动后端服务
cd backend && ./scripts/run-services.sh

# 启动前端服务
cd frontend/wms-portal && pnpm dev
```

### 访问地址
- **前端界面**: http://localhost:5173
- **API网关**: http://localhost:9000
- **认证服务**: http://localhost:9001

### 默认账号
- 用户名: `admin`
- 密码: `Admin@123`

## 📋 功能特性

### 🔐 用户认证与权限管理
- 基于Sa-Token的登录认证
- RBAC角色权限控制
- JWT Token管理
- 多租户支持

### 📊 仓库管理
- 多仓多货主管理
- 物料与SKU管理
- 库区库位配置
- 实时库存监控

### 🔄 入库出库流程
- 预约与ASN管理
- 波次拣选策略
- 自动化上架下架
- 包装复核流程

### 📈 可视化监控
- 实时KPI仪表板
- 3D/2D仓图展示
- 设备状态监控
- 告警中心

### 🔗 系统集成
- ERP/MES/OMS接口
- TMS承运商对接
- IoT设备接入
- BI报表导出

## 🛠️ 开发指南

### 项目结构
```
backend/
├── common/core/               # 公共配置和工具类
├── system/system-auth/        # 用户认证服务
└── system/system-gateway/     # API网关服务

frontend/wms-portal/
├── src/
│   ├── api/                   # API接口
│   ├── components/            # 公共组件
│   ├── layouts/               # 页面布局
│   ├── router/                # 路由配置
│   ├── stores/                # 状态管理
│   └── views/                 # 页面视图
└── docs/                      # 项目文档
```

### API文档
- 认证接口: `POST /api/auth/login`
- 用户信息: `GET /api/auth/profile`
- 菜单权限: `GET /api/auth/menus`

### 数据库设计
- 用户表: `sys_user`
- 角色表: `sys_role`
- 菜单表: `sys_menu`
- 用户角色关联: `sys_user_role`
- 角色菜单关联: `sys_role_menu`

## 📄 许可证

本项目采用 MIT 许可证。

## 🤝 贡献

欢迎提交Issue和Pull Request来改进这个项目。

## 📞 联系方式

如有问题请通过以下方式联系：
- 项目维护者: WMS Team
- 邮箱: developer@wms-system.com
